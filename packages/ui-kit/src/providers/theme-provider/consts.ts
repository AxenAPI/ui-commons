import { default as DEFAULT_THEME } from '@/assets/themes/default.json';

export const THEME_LOCAL_STORAGE_PROP_DEFAULT_NAME = 'theme';

export const DEFAULT_THEME_ID = DEFAULT_THEME.id;

export { DEFAULT_THEME };
