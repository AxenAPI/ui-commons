import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { Tabs } from '../';
import * as TabsStories from './Tabs.stories';

<Meta of={TabsStories} />

<Title />

Tabs - это компонент, который позволяет организовать контент во вкладках, обеспечивая удобную навигацию

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
