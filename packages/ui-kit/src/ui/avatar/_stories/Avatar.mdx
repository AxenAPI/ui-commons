import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as AvatarStories from './Avatar.stories';

<Meta of={AvatarStories} />

<Title />

Avatar — это компонент, который используется для отображения изображения пользователя, иконки или инициала. Он может быть круглым или квадратным, а также иметь разные размеры.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
