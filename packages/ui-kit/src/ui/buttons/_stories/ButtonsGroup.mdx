import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { ButtonsGroup } from '../ButtonsGroup';
import * as ButtonsGroupStories from './ButtonsGroup.stories';

<Meta of={ButtonsGroupStories} />

<Title />

ButtonsGroup - это компонент, объединяющий кнопки в группу.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
