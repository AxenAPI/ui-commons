import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as ModalStories from './Modal.stories';

<Meta of={ModalStories} />

<Title />

Modal — это элемент пользовательского интерфейса, который отображает содержимое во всплывающем окне поверх основного контента приложения.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
