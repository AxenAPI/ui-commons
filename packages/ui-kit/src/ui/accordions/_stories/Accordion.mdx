import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { Accordion } from '../accordion';
import * as AccordionStories from './Accordion.stories';

<Meta of={AccordionStories} />

<Title />

Accordion - это компонент, который реализует функциональность области контента, которую можно сворачивать и расширять. В режиме Accordion возможно расширять только один элемент.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
