import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as PaginationStories from './Pagination.stories';

<Meta of={PaginationStories} />

<Title />

Pagination - это компонент, который позволяющий пользователю выбрать определенную страницу из ряда страниц.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
