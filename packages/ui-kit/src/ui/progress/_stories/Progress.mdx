import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as ProgressStories from './Progress.stories';

<Meta of={ProgressStories} />

<Title />

Progress - это компонент, позволяющий отобразить пользователю состояние прогресса выполнения какой-либо операции.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
