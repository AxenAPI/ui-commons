import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as TextAreaStories from './TextArea.stories';

<Meta of={TextAreaStories} />

<Title />

TextArea - это компонент, который используется для отображения текста, а также для редактирования и получения введенного текста.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
