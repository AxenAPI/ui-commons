import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as FormItemStories from './FormItem.stories';

<Meta of={FormItemStories} />

<Title />

FormItem - это компонент пользовательского интерфейса, позволяющий добавлять элементы в форму

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
