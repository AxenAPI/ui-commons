import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { ScrollArea } from '../ScrollArea';
import * as ScrollAreaStories from './ScrollArea.stories';

<Meta of={ScrollAreaStories} />
<Title />

ScrollArea - это компонент, который добавляет скроллу кросс-браузерную кастомизацию.

<Primary />

<Controls />

<Stories />
