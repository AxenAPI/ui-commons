import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as SegmentedStories from './Segmented.stories';

<Meta of={SegmentedStories} />

<Title />

Segmented — это элемент пользовательского интерфейса, который обеспечивает отображение нескольких вариантов и предоставление пользователям возможности выбрать один вариант.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
