import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as PageSpinnerStories from './PageSpinner.stories.tsx';

<Meta of={PageSpinnerStories} />

<Title />

PageSpinner — это компонент который используется в качестве индикатора загрузки.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
