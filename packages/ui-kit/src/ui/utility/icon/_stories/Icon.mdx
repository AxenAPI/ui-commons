import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

<Title />

Icon - это графический элемент, который используется для отображения небольшой изображения или символа в приложении.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
