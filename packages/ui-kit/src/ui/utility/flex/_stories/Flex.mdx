import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as FlexStories from './Flex.stories';

<Meta of={FlexStories} />

<Title />

Flex - компонент-контейнер для позиционирования потомков с помощью dislay: flex.

## Пример использования компонента:

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
