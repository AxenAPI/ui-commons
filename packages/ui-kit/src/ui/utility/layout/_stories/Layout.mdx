import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as LayoutStories from './Layout.stories';

<Meta of={LayoutStories} />

<Title />

Layout — это компонент общего макета страницы.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
