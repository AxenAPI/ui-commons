import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as SpaceStories from './Space.stories';

<Meta of={SpaceStories} />

<Title />

Space - это компонент, который используется для добавления пространства между элементами интерфейса, помогает избегать прилипания компонентов друг к другу, а также позволяет легко управлять вертикальными и горизонтальными отступами между компонентами.

## Пример использования компонента:

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
