import { Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';

import * as ColStories from './Col.stories';

<Meta of={ColStories} />

<Title />

Col - это компонент, который используется для определения столбцов внутри строки (Row) сетки. Он позволяет контролировать размеры столбца, его позицию и поведение на разных устройствах или при разной ширине экрана.

## Пример использования компонента:

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
