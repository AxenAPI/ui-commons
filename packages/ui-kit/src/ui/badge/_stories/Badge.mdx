import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { Badge } from '../';
import * as BadgeStories from './Badge.stories';

<Meta of={BadgeStories} />

<Title />

Badge - это компонент, который представляет собой небольшое числовое значение или дескриптор статуса для элементов пользовательского интерфейса..

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
