import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { CheckboxField } from '../CheckboxField';
import * as CheckboxFieldStories from './CheckboxField.stories';

<Meta of={CheckboxFieldStories} />

<Title />

CheckboxField - это компонент Checkbox, адаптированный для работы с react-hook-form.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
