import { Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';

import { SelectField } from '../SelectField';
import * as SelectFieldStories from './SelectField.stories';

<Meta of={SelectFieldStories} />

<Title />

SelectField - это компонент Select, адаптированный для работы с react-hook-form.

<Primary />

Компонент принимает следующие параметры (props):

<Controls />

<Stories />
